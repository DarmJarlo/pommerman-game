Setup a Anaconda/Miniconda Environment:
	conda create -n pommerman python=3.7

From the base folder install Pommerman and all dependecies with:
	pip install -U .
	
To run and train the agent against the SimpleAgent simply run "python run.py".
